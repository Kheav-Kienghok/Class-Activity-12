server {
    listen 80;

    location / {
        default_type text/html;
        return 200 "<h1>nginx is running</h1>";
    }

    location /stub_status {
        stub_status;
        allow all;
    }
}
